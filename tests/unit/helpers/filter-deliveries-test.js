
import { filterDeliveries } from 'ga-wdi-boston.planning-genie-client/helpers/filter-deliveries';
import { module, test } from 'qunit';

module('Unit | Helper | filter deliveries');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterDeliveries([42]);
  assert.ok(result);
});

